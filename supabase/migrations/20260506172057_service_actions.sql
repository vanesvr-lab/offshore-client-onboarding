-- B-072 Batch 1 — Empty placeholder (CLI race: `npx supabase migration new`
-- created this stub and `npm run db:push` ran it before the SQL was
-- written. The actual DDL lives in 20260506172151_service_actions_tables.sql
-- (next migration). Keeping this file recorded in the migration ledger so
-- Local + Remote stay paired.

SELECT 1;
